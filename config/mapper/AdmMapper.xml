<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project2.dao.AdmDao">

    <select id="queryAdm" resultType="Adm">
        SELECT * FROM adms WHERE name=#{name} AND password=#{password}
    </select>
</mapper>